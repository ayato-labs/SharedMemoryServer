services:
  ripen:
    image: ghcr.io/ayato-labs/ripen:latest
    ports:
      - "8377:8377"
    volumes:
      - ./data:/data
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - LOG_LEVEL=INFO
    restart: unless-stopped
